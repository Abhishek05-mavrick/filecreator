
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
    <title>Document</title>
</head>
<body>
   <div class="main w-full h-screen bg-slate-600">
    <div class="form px-10 py-8 text-white">
        <form action="/create" method="post">
            <input class="block bg-slate-700 px-4 py-2 w-full mt-2 rounded-md border-none" name="title" type="text" placeholder="Task Title">
            <textarea class="block bg-slate-700 px-4 py-2 w-full rounded-md mt-2 resize-none border-none" name="describe" placeholder="Add tasks here"></textarea>
            <input class="h-14 bg-teal-700 mt-2 rounded-lg w-28 hover:bg-slate-800" type="submit" value="Create Task">
        </form>
    </div>
    <div class="table flex w-full h-3/5 p-9">
        <% if(files.length > 0){ %>
            <% files.forEach(function (val){ %>
                <div class="w-full bg-slate-800 px-5 py-3 mt-2 text-white rounded-lg text-xl">
                    <%= val %>
                    <a href="/files/<%= val %>" class="text-teal text-right">...Read more</a>
                </div> 
                <div><button class="bg-teal-700 justify-self-end hover:bg-slate-800 mt-2">Clear</button></div>
            <% }) %>
        <% } else { %>
            <h1>No tasks yet...</h1>
        <% } %>
    </div>
   </div>


    




    <script src="/javascripts/index.js"></script>
</body>
</html>